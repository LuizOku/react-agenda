import styled from 'styled-components';

export const Button = styled.button`
  height: 35px;
  background: #006064;
  border-radius: 15px;
  border: none;
  color: #FFF;
  cursor: pointer;
  &:hover {
    transform: scale(1.05);
  }
`;
